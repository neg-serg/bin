function sprunge() {
    if [[ -t 0 ]]; then
      if [[ "$*" ]]; then
        if [[ -f "$*" ]]; then
          cat "$*"
        else
          echo "$*"
        fi | curl -F 'sprunge=<-' http://sprunge.us
      fi
    else
      curl -F 'sprunge=<-' http://sprunge.us
    fi
}

sprunge "$@"
